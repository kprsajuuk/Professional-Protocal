# features — 业务域模块（预留）

本目录用于**按业务域聚合**前端代码（页面、组件、hooks、该域专属的 service 调用等），
例如未来的 `contacts/`（联系人）、`interactions/`（互动记录）等。

当前为空——业务需求确定后再逐步填充。通用基础设施在 `src/` 下的 `api/`、`hooks/`、`lib/`、`utils/`、`layouts/` 等目录。
